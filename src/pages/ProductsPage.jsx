import ProductSection from '../components/ProductSection.jsx';

function ProductsPage({ content, language }) {
  return <ProductSection content={content} language={language} />;
}

export default ProductsPage;
